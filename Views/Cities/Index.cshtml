@{
  Layout = "_Layout";
}

@using LocalLookupMVC.Solution.Models;
@model List<LocalLookupMVC.Solution.Models.City>;

<div class='jumbotron'>
 <h1>Citys</h1>
</div>
<div>
  <div class='entryLabel'>
    <h2>
      Index
    </h2> 
  </div>
  <div class='entry'>
    @if (@Model.Count == 0)
    {
      <h3>No Citys have been added yet!</h3>
    } 

    @foreach (City City in Model)
    {
      <li>@Html.ActionLink($"{City.Name}", "Details", new { id = City.CityId })</li>
    }
    
    @if(User.Identity.IsAuthenticated)
    {
      <p>@Html.ActionLink("Add new City", "Create")</p>
    }
        <li>Page: @ViewBag.page</li>
         <p>@Html.ActionLink("Back a Page", "Index", "Citys", new{page = ViewBag.page - 1})</p>
         
         <p>@Html.ActionLink("1", "Index", "Citys", new{page = ViewBag.page, pageCount = 1 })</p>
         
         <p>@Html.ActionLink("2", "Index", "Citys", new{page = ViewBag.page, pageCount = 2})</p>
         
         <p>@Html.ActionLink("3", "Index", "Citys", new{page = ViewBag.page, pageCount = 3})</p>
         <p>@Html.ActionLink("Next Page", "Index", "Citys", new{page = ViewBag.page + 1})</p>
  </div>
</div>
